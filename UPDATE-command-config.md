# Update Notes: Commands & Config

## English
- Expanded README command guide for `/p2pdetails scan` with submodes: `summary`, `isolated`, `unconnected`, `full`, `near`, `all`.
- Added clear argument rules: `mode` (optional), `limit` (optional, `1-50`), and default behavior.
- Added bilingual examples for common scan usage.
- Added config file section in README: file paths, option meanings, and reload behavior.
- In-game config changes are now synced at runtime (no full game restart required).

## 中文
- README 已补充 `/p2pdetails scan` 的子命令说明：`summary`、`isolated`、`unconnected`、`full`、`near`、`all`。
- 已补充参数规则：`mode`（可选）、`limit`（可选，`1-50`）及默认行为。
- 已补充常用扫描命令示例（中英双语）。
- README 已新增配置文件说明：路径、配置项含义、生效方式。
- 游戏内修改配置现在可运行时同步（无需重启整个游戏）。
